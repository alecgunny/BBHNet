[tool.poetry]
name = "aframe"
version = "0.0.1"
description = "aframe utilities"
authors = ["Alec Gunny"]
license = "MIT"
# readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.8,<3.12"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
